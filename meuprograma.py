age = input("Enter your age: ") 

# The input function catchs a string in every case and we have
# to convert the vlue typed by the user
						  
#new_age = age + 50  /// We have to convert the age of string for int
new_age = int(age) + 50
print(new_age)
